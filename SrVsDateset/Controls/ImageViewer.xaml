<UserControl x:Class="SrVsDataset.Controls.ImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SrVsDataset.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="Black" ClipToBounds="True">
        <ScrollViewer x:Name="ImageScrollViewer" 
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      PanningMode="Both"
                      CanContentScroll="True">
            <Image x:Name="PreviewImage" 
                   Source="{Binding RelativeSource={RelativeSource AncestorType=local:ImageViewer}, Path=ImageSource}"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
        </ScrollViewer>
    </Border>
</UserControl>